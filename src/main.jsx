import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./styles/index.css";

// Mount the standalone editor into Vite's root element.
ReactDOM.createRoot(document.getElementById("root")).render(
  <App />,
);
